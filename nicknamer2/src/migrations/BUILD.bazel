load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "migrations",
    srcs = ["migrations.rs"],
    compile_data = [
        "001_create_users_table.sql",
        "002_create_names_table.sql",
    ],
    visibility = ["//nicknamer2:__subpackages__"],
    deps = ["@crates//:sqlx"],
)

rust_test(
    name = "migrations_test",
    timeout = "short",
    crate = ":migrations",
    tags = [
        "requires-network",
    ],
    deps = [
        "@crates//:testcontainers-modules",
        "@crates//:tokio",
    ],
)
