ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ io.lowkeylab.mindreadr.app.ApplicationKt.module ]
    }
}

frontend {
  url = "localhost:4200"
  url = ${?FRONTEND_URL}
}
