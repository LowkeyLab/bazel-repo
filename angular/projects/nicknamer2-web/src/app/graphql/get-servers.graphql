query GetServers($first: Int!, $after: String) {
  servers(first: $first, after: $after) {
    edges {
      cursor
      node {
        id
        serverId
        displayName
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
