load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "lib",
    srcs = ["lib.rs"],
    deps = [
        "//nicknamer/server/lib/templates",  # keep
        "@crates//:anyhow",
        "@crates//:serde",
    ],
)
