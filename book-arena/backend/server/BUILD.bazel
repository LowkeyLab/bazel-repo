load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//book-arena"])

rust_library(
    name = "lib",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "book_arena_migration",
    deps = [
        "@crates//:sea-orm-migration",
        "@crates//:tokio",
    ],
)
