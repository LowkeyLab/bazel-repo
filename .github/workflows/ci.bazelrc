# Allows tests to run bazelisk-in-bazel, since this is the cache folder used
test --test_env=XDG_CACHE_HOME

# Use remote execution on CI
common --config=remote-linux
# Use settings for CI environment
common --config=ci
